kind: Authorize
apiVersion: api.ibm.com/v1
metadata:
  name: Authorize-P3XmK
  namespace: risk-api
  version: '1.0'
  tags: []
  labels:
    gatewayTypes:
      - nano
spec:
  operation:
    oauth2:
      issClaim: sub
      requiredScopes:
        - provider: verify-joy
          scopes:
            - loanadmin
  namespace: risk-api
