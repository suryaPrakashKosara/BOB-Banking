kind: Authorize
apiVersion: api.ibm.com/v1
metadata:
  name: Authorize-41e9F
  namespace: payment-service
  version: '1.0'
  tags: []
  labels:
    gatewayTypes:
      - nano
spec:
  operation:
    oauth2:
      issClaim: sub
      requiredScopes:
        - provider: verify-joy
          scopes:
            - customer
  namespace: payment-service
