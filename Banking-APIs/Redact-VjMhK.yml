kind: Redact
apiVersion: api.ibm.com/v1
metadata:
  name: Redact-VjMhK
  namespace: Banking-APIs
  version: '1.0'
  tags: []
  labels:
    gatewayTypes:
      - nano
spec:
  message:
    messageName: response
    body:
      captures:
        jsonata:
          - $.aadhaar
          - $.email
          - $.pan
          - $.mobileNumber
          - customers.email
          - customers.mobileNumber
          - customers.pan
          - customers.aadhaar
