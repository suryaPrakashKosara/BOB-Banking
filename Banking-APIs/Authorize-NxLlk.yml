kind: Authorize
apiVersion: api.ibm.com/v1
metadata:
  name: Authorize-NxLlk
  namespace: Banking-APIs
  version: '1.0'
  tags: []
  labels:
    gatewayTypes:
      - nano
spec:
  namespace: Banking-APIs
  operation:
    oauth2:
      issClaim: 'sub '
      requiredScopes:
        - provider: verify-joy
          scopes:
            - customer
