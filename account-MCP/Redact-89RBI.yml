kind: Redact
apiVersion: api.ibm.com/v1
metadata:
  name: Redact-89RBI
  namespace: account-MCP
  version: '1.0'
  tags: []
  labels:
    gatewayTypes:
      - nano
spec:
  message:
    messageName: response
    body:
      captures:
        jsonata:
          - $. accountNumber
          - accounts.accountNumber
