kind: Authorize
apiVersion: api.ibm.com/v1
metadata:
  name: Authorize-mTE0w
  namespace: account-MCP
  version: '1.0'
  tags: []
  labels:
    gatewayTypes:
      - nano
spec:
  namespace: account-MCP
  operation:
    oauth2:
      issClaim: 'sub '
      requiredScopes:
        - provider: verify-joy
          scopes:
            - bankmanager
            - customer
